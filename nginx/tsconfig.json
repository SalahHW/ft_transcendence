{
	"compilerOptions": {
		"outDir": "./public/js/",
		"module": "ES2020",
		"moduleResolution": "node",
		"strict": false, // active tous les flags strict
		"noImplicitAny": true, // signale les types implicites
		"noImplicitReturns": true, // signale les fonctions sans return
		"noFallthroughCasesInSwitch": true, // signale les cas de switch sans break
        "noUnusedLocals": false, // signale les variables non utilisées
        "noUnusedParameters": false, // signale les paramètres non utilisés
        "forceConsistentCasingInFileNames": true, // assure une casse(=>MAJ/min) cohérente
		"skipLibCheck": true, // ignore les vérifications de type dans les fichiers de déclaration
		"lib": ["dom", "dom.iterable", "es2019"],
	},
	"include": [
		"./src/*.ts",
		"./src/*/*.ts",
		"./src/*/*/*.ts"
	]
}
